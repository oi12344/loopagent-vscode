# 项目规则

## 文档语言

项目文档必须使用中文输出。

适用范围包括：

- 设计文档
- 实施计划
- 开发指南
- 架构决策记录
- 功能说明
- 验证记录
- 技术债说明

代码标识符、命令、文件路径、API 名称、错误原文可以保留英文。

## 开发文档

任何功能、行为变更或架构决策，必须同步落地对应文档。没有文档的功能不能视为完成。

使用以下目录：

- `docs/superpowers/specs/`：设计规格、架构决策、重要取舍。
- `docs/superpowers/plans/`：实施计划、任务拆解、验收步骤。
- `docs/development.md`：稳定的项目级开发规范。

每次功能变更必须做到：

1. 新增或更新设计/计划文档，说明目标、范围、取舍和验证方式。
2. 在文档中关联相关源码、测试、命令或后续事项。
3. 如果实现方向发生变化，及时更新文档。
4. 不把关键行为、临时假设或隐藏决策只留在代码里。

## 代码质量

代码必须保持简洁，变更范围要贴近当前功能。

- 优先使用小函数，输入输出清晰。
- 至少出现两个真实调用点之前，不提前抽象。
- 不为猜测中的未来需求添加框架胶水、通用工具层或复杂封装。
- 用户界面逻辑和 agent runtime 逻辑要保持边界清晰。
- 优先写可读的 TypeScript，不追求炫技写法。

## 开发分支与 worktree

除非用户主动明确要求在 worktree 中开发，否则直接在当前 checkout、当前分支完成开发，不得自行创建或切换 worktree。该偏好优先于工具或技能提供的默认 worktree 建议。

## 统一开发流程

所有代码变更统一按以下顺序执行；技能只补充当前步骤的做法，不额外增加并行流程或重复门禁：

1. **检查状态**：确认当前分支、worktree、用户已有改动、相关入口和现有测试。只读问题停在这一步，不顺带修改代码。
2. **确定范围**：写清调用方、成功结果、必须避免的失败。单文件窄改动直接进入实现；跨模块、持久化、并发或公共契约变更，先在 `docs/superpowers/specs/` 写简短设计，再在 `docs/superpowers/plans/` 写实施计划。
3. **打通闭环**：按真实用户路径实现最小纵向切片。新行为先写一个能失败的核心测试；不为中间层堆测试、抽象或脚手架。
4. **集中验证**：先运行受影响测试和类型检查；闭环完成后再运行一次全量测试、构建和 `git diff --check`。独立命令并行执行，失败只修复根因并重新验证受影响范围。
5. **清理交付**：删除死代码、临时调试和过期文档，更新设计/计划中的完成记录，集中审查 diff，然后提交。清理存在风险时记录为技术债；除非用户明确要求，不自动推送、合并或删除分支。

沟通规则：命令完成后立即报告结果；命令超过 30 秒主动说明正在等待什么；不重复读取未变化内容，不在工具调用之间静默执行长时间分析。

## VS Code 本地调试窗口规则

1. 本项目调试 VS Code 插件时，默认只允许存在一个 LoopAgent Extension Development Host 窗口。
2. 启动本地调试必须优先使用 `npm run debug:vscode`，不要反复手写 `code --extensionDevelopmentPath` 命令。
3. 调试入口必须复用固定目录 `.local-vscode-user-data`、`.local-vscode-extensions` 和固定远程调试端口，避免每次测试生成新的临时 VS Code 配置目录。
4. 如果确实需要并行打开第二个调试窗口，必须先说明原因，并在测试结束后关闭额外窗口、清理临时目录。
5. 新需求完成后的本地验证，应在同一个调试窗口内完成：刷新窗口、重新执行 `LoopAgent: Open Panel`，而不是重复启动多个 VS Code 实例。
