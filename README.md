# LoopAgent VS Code Extension

Minimal TypeScript VS Code extension scaffold for LoopAgent development.

## Development

```powershell
npm install
npm run compile
```

Open this folder in VS Code, press F5, then run `LoopAgent: Hello` from the command palette in the Extension Development Host.
