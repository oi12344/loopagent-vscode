# 旧 Superpowers 运行时移除计划

## 成功标准

仓库不再编译、测试或打包旧 Superpowers 运行时；ReAct runner、新子代理工作流、普通中断恢复和 VSIX 生产负载保持可用。

## 执行步骤

1. 更新 provider 与 VSIX 契约测试，确认缺失模块和旧资源脚本导致 RED。
2. 清理 provider 旧分支、checkpoint 类型与存储、vendoring 和打包逻辑。
3. 删除旧运行时代码、资源及其专用测试，移除已跳过的恢复用例。
4. 搜索残留引用，保留仅用于迁移说明和打包排除的路径文本。
5. 执行定向测试、类型检查、编译、全量测试、VSIX 打包和差异检查。
6. 集中审查最终差异，提交当前本地变更并推送 `origin/main`。

## 完成记录

- [x] provider 与 VSIX 契约 RED
- [x] 生产入口、checkpoint 和打包残留清理
- [x] 旧资源、运行时和专用测试删除
- [x] 中文设计与计划记录
- [x] 全量验证：`504` 个测试、类型检查、编译、VSIX 打包和差异检查通过
- [x] 独立代码审查通过，具备提交与推送条件
